<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="library.mapper.UserMapper">
	<resultMap id="UserResult" type="library.dto.UserDto">
        <result column="user_id" property="userId" />
        <result column="user_name" property="userName" />
        <result column="library_id" property="libraryId" />
        <result column="insert_time" property="insertTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
	<insert id="insert">
		INSERT INTO user (user_id, user_name, address, mail, phoneNumber, library_id, insert_time, update_time) 
		 VALUES (#{userId}, #{userName}, #{address}, #{mail}, #{phoneNumber}, #{libraryId}, #{insertTime}, #{updateTime}) ;
	</insert>
	
	<select id="search" resultMap="UserResult">
		SELECT * FROM user WHERE user_id = #{userId} ;
	</select>
	
	<update id="update">
		UPDATE user SET user_id = #{userId}, user_name = #{userName}, address = #{adress}, mail = #{mail},
		 phoneNumber = #{phoneNumber}, library_id #{libraryId}, insert_time = #{insertTime}, update_time = #{updateTime} 
		 WHERE user_id = #{userId}
	</update>
	

	
	
</mapper>
